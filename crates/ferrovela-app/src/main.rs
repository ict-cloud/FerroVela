fn main() {
    // Thin entry point for cargo-bundle.
    // The actual binaries (ferrovela, ferrovela-ui) are copied into the
    // app bundle by bundle.sh after cargo-bundle creates the .app structure.
}
